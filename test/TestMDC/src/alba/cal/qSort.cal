package alba.cal;

actor qSort(int DATA_SIZE=32,int ADDR_SIZE=16,int SIZE_V=25,int LOG2_SIZE_V=5) int(size=DATA_SIZE) in1, int(size=2*DATA_SIZE+2*ADDR_SIZE) value ==> int(size=DATA_SIZE) out, int(size=DATA_SIZE) result :

end
