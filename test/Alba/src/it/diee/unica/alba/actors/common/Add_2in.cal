package it.diee.unica.alba.actors.common;


actor Add_2in(int DATA_SIZE=32) int(size=DATA_SIZE) in1, int(size=DATA_SIZE) in2 ==> int(size=DATA_SIZE) out :

	action in1:[a], in2:[b] ==> out:[a+b]
	do
		//println("adding " + a + "+" + b + "=" + (a+b));
	end
		
end
