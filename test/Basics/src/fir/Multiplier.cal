package fir;

actor Multiplier(int SIZE=32)
	int(size=SIZE) in1,
	int(size=SIZE) in2
		 ==>
	int(size=2*SIZE) out1
:

add: action in1:[a], in2:[b] ==> out1:[a*b]
do
	//println("Multiplying " + a + "*" + b + "=" + (a*b));
end



end