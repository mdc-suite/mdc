package fir;

@combinational
actor Adder(int SIZE=64)
	int(size=SIZE) in1, 
	int(size=SIZE) in2
		 ==>
	int(size=SIZE) out1
:

add: action in1:[a], in2:[b] ==> out1:[a+b]
do
	//println("Adding " + a + "+" + b + "=" + (a+b));
end



end